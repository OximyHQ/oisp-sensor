<Application x:Class="OISPApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown"
             Startup="Application_Startup"
             Exit="Application_Exit">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Tray Icon -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="/Resources/oisp-icon.ico"
                            ToolTipText="OISP Sensor - Stopped"
                            MenuActivation="LeftOrRightClick"
                            DoubleClickCommand="{x:Static local:App.OpenSettingsCommand}"
                            xmlns:local="clr-namespace:OISPApp">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="OISP Sensor" FontWeight="Bold" IsEnabled="False"/>
                        <Separator/>
                        <MenuItem Header="_Start Capture" Click="StartCapture_Click" Name="StartMenuItem"/>
                        <MenuItem Header="S_top Capture" Click="StopCapture_Click" Name="StopMenuItem" IsEnabled="False"/>
                        <Separator/>
                        <MenuItem Header="_Install CA Certificate" Click="InstallCA_Click"/>
                        <MenuItem Header="Show CA _Location" Click="ShowCALocation_Click"/>
                        <Separator/>
                        <MenuItem Header="Se_ttings..." Click="Settings_Click"/>
                        <MenuItem Header="View _Logs" Click="ViewLogs_Click"/>
                        <Separator/>
                        <MenuItem Header="E_xit" Click="Exit_Click"/>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
